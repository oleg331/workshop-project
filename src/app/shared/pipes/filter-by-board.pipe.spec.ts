import { FilterByBoardPipe } from './filter-by-board.pipe';

describe('FilterByBoardPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBoardPipe();
    expect(pipe).toBeTruthy();
  });
});
